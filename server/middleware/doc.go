// Package middleware holds Echo middleware for the server — notably the
// Keycloak JWT authentication middleware that validates bearer tokens against
// the realm JWKS and resolves the token subject to a local user. Empty for now.
package middleware
